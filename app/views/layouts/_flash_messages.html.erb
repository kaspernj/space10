<% flash.each do |name, msg| %>
  <% unless Rails.env.production? %>
    <%= content_tag(:div, id: "flash_#{name}", class: "alert flash_#{name}") do %>
      <%= content_tag(:div, msg, class: "container") %>
    <% end %>
  <% end %>
<% end %>