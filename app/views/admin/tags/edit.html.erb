<% provide(:title, "Edit tag") %>
<div class="row">
  <div class="col-md-12">
    <h2>Edit tag</h2>
    <%= simple_form_for @tag, url: admin_tag_path(@tag) do |f| %>
      <%= render "form_fields", f: f %>
      <%= f.submit class: 'btn btn-default' %>
    <% end %>
  </div>
</div>